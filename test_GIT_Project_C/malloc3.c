#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a[3];
    int *ptr = a;

    printf("Enter 3 numbers: ");
    scanf("%i", a);

    ptr = (*int) malloc(sizeof(int)*N)

    printf("%i", ptr[i]);



    return 0;
}
