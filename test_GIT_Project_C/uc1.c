#include <stdio.h>
int main()
{
  int k, l, choice;
  float entered, converted;
  float kilo = 1, lbs = 2.2;

  printf("Enter weight: ");
  scanf("%f", &entered);

  printf("Kilo or LBS? \n");
  printf("l = LBS, k = Kilo: ");
  scanf("%i %i", &k, &l);

    if (k);
    {
        printf("%f %f\n", entered, kilo);
    }
    else if (l);
    {
        printf("%f %f\n", entered, lbs);
    }
    else



    return 0;

}
