#include <stdio.h>
int main()
{
    int input;
    char yesno;

    printf("How many bikes you do PERSONALLY own: " );
    scanf("%i", &input);

    if (input > 1)          {
        printf("You own more than one bike yourself? FUCKIN' NIGGER\n" );
    }
    else if (input == 1)    {
        printf("Get into the Winchester, have a pint, and wait for all this to blow over\n" );
    }
    else if (input == 0)    {
        printf("Have you ever owned a bike? [Y/N]: " );
        scanf("%s", &yesno);
                    if (yesno == 'y' || yesno == 'Y')          {
                        printf("YOUR BIKE HAS BEEN STOLEN BY NIGGERS\n");
                }
                    else if (yesno != 'y' || yesno != 'Y')     {
                        printf("You're most likely fat. Get some exercise\n");
                }
                    else ;
    }
    else
    return 0;
}
