#include <stdio.h>
int main()
{
    int *array, max, size, i;
    float std, mean, sum = 0;

    printf("Please enter size of array: \n");
    scanf("%i", &array);

    array = (size *) malloc(sizeof(array));

    for (i=0;i<size;i++)
    {
      printf("Enter values for the array: ");
      scanf("%i", &array);
      sum = size / array;
    }








    return;
}
